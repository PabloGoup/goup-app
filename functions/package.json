{
    "name": "functions",
    "private": true,
    "main": "lib/index.js",
    "engines": { "node": "20" },
    "scripts": {
      "build": "tsc -p .",
      "clean": "rimraf lib",
      "watch": "tsc -w -p .",
      "deploy:order": "npm run build && firebase deploy --only functions:onOrderWrite"
    },
    "dependencies": {
      "firebase-admin": "^13.5.0",
      "firebase-functions": "^6.4.0"
    },
    "devDependencies": {
      "@types/node": "^20.11.0",
      "rimraf": "^6.0.1",
      "typescript": "^5.9.2"
    }
}